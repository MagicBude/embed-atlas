# AGENTS.md

本文件是所有开发者和 AI 助手进入 EmbedAtlas 仓库后的首要工作说明。其约束适用于整个仓库；如果子目录以后出现更具体的 `AGENTS.md`，则子目录规则可以在不违背本文件原则的前提下补充本规则。

## 1. 项目使命

EmbedAtlas 是面向中文嵌入式工程师的开源知识库与在线工具箱。项目优先保证：

1. 内容正确并且可以追溯来源。
2. 解释清楚，能够帮助读者真正理解。
3. 计算工具的结果可以验证。
4. 架构模块化，便于长期增加、替换和删除模块。
5. 在多个设备、开发者和 AI 助手之间能够连续工作。

## 2. 开始工作前必须阅读

按以下顺序读取：

1. 本文件 `AGENTS.md`。
2. 根目录 `PROJECT_STATUS.md`：快速了解阶段、里程碑和阻塞。
3. `docs/project/status.md`：确认详细完成项和近期行动。
4. `docs/work-log.md`：了解最近执行事实与验证结果。
5. `docs/roadmap.md`：确认本次工作属于哪个阶段。
6. `docs/architecture/overview.md`：确认模块边界。
7. 与任务相关的 `docs/standard/` 和 `docs/architecture/` 文档。
8. `CHANGELOG.md` 与 `git status`：确认已有变更，避免覆盖他人工作。

不能因为仓库暂时简单而跳过这些入口。

## 3. 文档的职责与优先级

| 文件或目录 | 职责 |
| --- | --- |
| `AGENTS.md` | 跨开发者、跨设备、跨 AI 的工作规则 |
| `PROJECT_STATUS.md` | 快速接续总览：阶段、里程碑和阻塞 |
| `docs/project/status.md` | 详细当前事实、进行中工作和最近下一步 |
| `docs/work-log.md` | 追加式执行事实、关键判断和验证结果 |
| `docs/roadmap.md` | 阶段目标和长期方向，不记录细碎任务 |
| `docs/architecture/` | 稳定的系统边界、模块设计和架构决策 |
| `docs/product/` | 产品信息架构、交互原则和功能路线 |
| `docs/research/` | 外部参考、竞品观察和调研证据 |
| `docs/standard/` | 代码、注释、文档和 Git 规范 |
| `CHANGELOG.md` | 面向使用者和贡献者的可见变更 |

如果文档发生冲突，优先遵循更具体、更新日期更近且职责正确的文档；仍然无法判断时，先停止扩大变更范围，并记录待确认问题。

## 4. 工作原则

- 先理解问题和现有设计，再修改文件。
- 一次变更只解决一个清楚的问题，避免顺手大规模重构。
- 不覆盖或删除无法确认来源的本地改动。
- 新方向先写入路线图或架构决策，不直接侵入现有模块。
- 优先使用静态、离线可运行的实现；需要后端或外部服务时必须先记录理由。
- 工具的核心计算与界面分离，核心逻辑必须可以单独测试。
- 核心逻辑返回稳定代码和结构化数据，不直接返回绑定某种语言的完整提示文字。
- 所有带单位的变量、输入和输出必须明确单位。
- 知识内容必须区分事实、工程经验和作者建议。
- 引用资料必须可追溯，禁止整段复制受版权保护的内容。

## 5. 教材级代码与注释

本项目把“学习”视为核心功能，因此关键代码的注释应达到教材级别。教材级不等于逐行翻译代码，而是要帮助读者理解：

- 为什么选择该算法或实现方式。
- 输入、输出、单位、有效范围和前置条件。
- 公式、协议规则或标准依据。
- 边界情况、常见误区和平台差异。
- 必要时给出简短示例或中间结果。

完整要求和示例见 `docs/standard/comment-guidelines.md`。任何新增计算工具都必须遵守该规范。

## 6. 变更后的同步要求

完成有实际意义的工作后，根据影响范围同步：

- 根据影响范围同步 `PROJECT_STATUS.md` 和 `docs/project/status.md`。
- 完成独立工作批次后在 `docs/work-log.md` 顶部追加记录。
- 用户可感知的新增、修改或修复写入 `CHANGELOG.md` 的 `[Unreleased]`。
- 架构方向变化必须增加或更新 ADR，不得只写在代码注释或聊天中。
- 新增知识分类或工具分类时更新对应架构文档和导航。
- 新命令、依赖或环境要求必须更新 `docs/development/getting-started.md`。

不要为了微小的错别字更新所有状态文件，避免制造无意义噪声。

## 7. 验证要求

在交付前执行与变更相称的检查：

- 文档：检查链接、标题层级、代码块和术语一致性。
- TypeScript/Vue：格式检查、类型检查、单元测试和构建。
- 计算工具：正常值、边界值、无效输入和公开参考向量。
- UI：至少检查桌面与窄屏布局、键盘操作和错误提示。

如果某项检查暂时无法执行，必须明确记录未验证的内容和原因。

## 8. Git 与外部操作

- 遵守 `docs/standard/git-commits.md`。
- 未经仓库所有者明确要求，不创建提交、不推送远端、不修改发布配置。
- 不提交密钥、令牌、本地路径、个人配置或生成产物。
- 不修改 Git 历史，不执行破坏性清理命令。

## 9. 当前阶段约束

当前处于 `Phase 1：最小可用网站`。在该阶段：

- 优先完成少而完整的知识内容和经过验证的工具，不追求数量。
- 每个工具必须先有需求规格和验收用例，再实现计算核心与界面。
- 新增站点能力应复用当前 VitePress、Vue 和 TypeScript 架构。
- 不提前实现账号、数据库、云同步或桌面应用。
- 不提前发布内容不足的空语言站；多语言扩展遵守 ADR-0002 的质量门槛。
- 架构方向发生变化时，先通过 ADR 说明理由和影响。
